import LinkWithIcon from "./LinkWithIcon";

const ProjectsCard = ({ name, img, alt, desc, github, live }) => {
  const isPng = img.toLowerCase().endsWith(".png");

  // Disable rendering of the card by returning null
  return null;

};

export default ProjectsCard;
